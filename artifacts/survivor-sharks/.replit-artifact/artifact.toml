kind = "web"
previewPath = "/"
title = "Survivor Sharks"
version = "1.0.0"
id = "artifacts/survivor-sharks"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20034

[services.development]
run = "pnpm --filter @workspace/survivor-sharks run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/survivor-sharks", "run", "build" ]
publicDir = "artifacts/survivor-sharks/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20034"
BASE_PATH = "/"
