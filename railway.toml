[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && cd artifacts/api-server && npm install && npm run build"

[deploy]
startCommand = "node artifacts/api-server/dist/index.js"
